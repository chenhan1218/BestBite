# BestBite - 食品庫存管理 PWA 項目規劃

## 📱 項目概述

**BestBite** 是一個行動優先的 Web App (PWA)，幫助用戶管理食品庫存並追蹤有效期限。

**核心流程：** 拍照 → AI 識別品名與效期 → 存入清單 → 有效期提醒

---

## ✨ 核心功能 (MVP)

### 1. 相機輸入
- 首頁只有一個巨大、易於點擊的按鈕
- 打開相機或上傳照片
- 支援 iOS 和 Android 原生相機功能

### 2. AI 處理 (Gemini API)
- 將圖片傳送至 Google Gemini Vision API
- 自動識別：
  - `product_name` (產品名稱，例如 "義美小泡芙")
  - `expiry_date` (標準化為 YYYY-MM-DD)
  - `confidence` (辨識信心度)
- 用戶可編輯確認結果後保存

### 3. 庫存儀表板 (Inventory Dashboard)
- 依據過期日自動排序的清單
- 🔴 **紅燈 (Red):** 7 天內過期 (置頂顯示，字體放大，標記「緊急」)
- 🟡 **黃燈 (Yellow):** 30 天內過期
- 🟢 **綠燈 (Green):** 30 天後過期 (安全範圍)
- 每個項目支援長按或滑動刪除、標記為已食用

---

## 🛠️ 技術棧

| 層級 | 技術 | 說明 |
|------|------|------|
| **前端框架** | Next.js 15 (App Router) | 伺服器組件 + App Router |
| **UI 框架** | React 19 | 最新穩定版 |
| **樣式** | Tailwind CSS | 快速開發，易於自訂 |
| **狀態管理** | React Context API | 簡單項目足夠，支援離線 |
| **後端/資料庫** | Firebase (Firestore) | 無伺服器，實時同步 |
| **身份認證** | Firebase Auth (暫不用) | 預留，初期使用本地存儲 |
| **AI 集成** | Google Gemini API | Vision 圖像識別 |
| **PWA** | next-pwa | 離線支援，app-like 體驗 |
| **圖像處理** | Sharp + Canvas API | 圖片壓縮、預覽 |
| **部署** | Vercel | Next.js 官方推薦 |

---

## 📊 數據模型

### Firestore 集合結構

```
users/
  {userId}/
    profile: {
      name: string
      created_at: timestamp
    }
    food_items/
      {itemId}: {
        product_name: string              // "義美小泡芙"
        expiry_date: string               // "2025-12-25"
        days_until_expiry: number         // 自動計算
        status: "red" | "yellow" | "green" // 基於過期天數
        image_url: string                 // 儲存在 Storage
        confidence: number                // AI 辨識信心度 (0-100)
        created_at: timestamp
        updated_at: timestamp
      }
    notifications/
      {notifId}: {
        food_item_id: string
        type: "urgent" | "warning" | "info"
        notified_at: timestamp
      }
```

### 狀態計算邏輯

```
today = 現在日期
days = expiry_date - today

if days <= 7:
  status = "red"      // 🔴 緊急
else if days <= 30:
  status = "yellow"   // 🟡 注意
else:
  status = "green"    // 🟢 安全
```

---

## 🎨 UI 設計原則

### 字體與間距
- **基礎字體大小:** 18px (body text)
- **按鈕文字:** 24px+
- **標題:** 32px+
- **行高:** 1.6-1.8 (增加閱讀舒適度)
- **邊距與間距:** 使用 16px 倍數 (16px, 32px, 48px)

### 觸控區域
- **最小尺寸:** 56px × 56px (遵循 Google Material Design)
- **主要按鈕:** 64px × 64px 以上
- **相機按鈕:** 屏幕中央，至少 80px × 80px

### 顏色與對比度
- **文字 vs 背景:** 7:1 對比度 (WCAG AAA)
- **避免純紅+純綠組合 (色盲考慮)**
  - 紅燈: #EF4444 + 圖示 (圓形 + 標記) + 文字「緊急」
  - 黃燈: #FBBF24 + 圖示 (三角形) + 文字「注意」
  - 綠燈: #22C55E + 圖示 (勾選) + 文字「安全」

### 交互設計
- **層級簡化:** 最多 2-3 個點擊步驟
- **清晰的視覺反饋:** 按鈕按下時顯著變化 (顏色變深 / 陰影)
- **無 Hover 依賴:** 完全支援觸摸 (無滑鼠)
- **語氣親切:** 「趁新鮮快吃！」、「再放一會兒」、「已安心存放」

### 語言與文案
- 使用繁體中文，親切、正面的語氣
- 避免技術術語
- 清晰的操作提示 (例如: 「長按可刪除」)

---

## 📁 項目文件結構

```
bestbite/
├── .env.local                    # 本地環境變數 (Git ignore)
├── next.config.js                # Next.js 配置 + PWA
├── tailwind.config.ts            # Tailwind 配置
├── tsconfig.json                 # TypeScript 配置
├── public/
│   ├── manifest.json             # PWA manifest
│   ├── icons/
│   │   ├── icon-192.png
│   │   ├── icon-512.png
│   │   └── favicon.ico
│   └── sw.js                      # Service Worker (next-pwa)
├── src/
│   ├── app/
│   │   ├── layout.tsx            # Root layout (global styles)
│   │   ├── page.tsx              # 首頁 (相機按鈕)
│   │   ├── inventory/
│   │   │   └── page.tsx          # 庫存儀表板
│   │   ├── api/
│   │   │   └── gemini/
│   │   │       └── route.ts      # Gemini API 代理
│   │   └── globals.css           # Global Tailwind styles
│   ├── components/
│   │   ├── Camera/
│   │   │   ├── CameraButton.tsx
│   │   │   ├── ImageUpload.tsx
│   │   │   └── ImagePreview.tsx
│   │   ├── Inventory/
│   │   │   ├── FoodItemCard.tsx
│   │   │   ├── FoodList.tsx
│   │   │   └── FilterTabs.tsx
│   │   ├── Modal/
│   │   │   └── ConfirmationModal.tsx
│   │   └── Common/
│   │       ├── Header.tsx
│   │       └── Loading.tsx
│   ├── lib/
│   │   ├── firebase.ts           # Firebase 初始化配置
│   │   ├── gemini.ts             # Gemini API 工具函數
│   │   ├── image.ts              # 圖片壓縮 / 預處理
│   │   ├── date.ts               # 日期計算工具
│   │   └── storage.ts            # 本地存儲工具 (IndexedDB)
│   ├── types/
│   │   └── index.ts              # TypeScript 類型定義
│   ├── context/
│   │   └── FoodContext.tsx        # 全局食品清單狀態
│   └── hooks/
│       ├── useFoodItems.ts        # 食品項目 CRUD hooks
│       └── useCamera.ts           # 相機功能 hooks
└── package.json
```

---

## 🔄 工作流程 (User Journey)

### 流程 1: 添加食品 (首頁)

```
用戶打開應用
    ↓
看到首頁: 巨大相機按鈕 + 快速統計 (X 個紅燈, X 個黃燈, X 個綠燈)
    ↓
點擊相機按鈕
    ↓
選擇拍照或上傳
    ↓
打開手機相機 (或檔案選擇器)
    ↓
選擇或拍攝食品包裝照片
    ↓
圖片上傳到 Gemini API
    ↓
顯示「識別中...」加載狀態
    ↓
AI 返回: 產品名稱 + 有效期限
    ↓
展示確認對話框:
  - 顯示識別的產品名稱 (可編輯)
  - 顯示有效期限 (可編輯)
  - 顯示原始圖片縮圖
  - 確認按鈕 / 取消按鈕
    ↓
用戶點擊「確認」
    ↓
保存到 Firestore
    ↓
返回首頁，顯示成功提示
    ↓
統計數字自動更新
```

### 流程 2: 查看清單 (庫存儀表板)

```
用戶點擊「查看清單」或統計數字
    ↓
進入庫存儀表板
    ↓
自動按照過期日期排序:
  🔴 紅燈區 (7天內過期) - 置頂，字體放大
  🟡 黃燈區 (7-30天)
  🟢 綠燈區 (30天以上)
    ↓
每個項目顯示:
  - 產品名稱 (大字)
  - 過期日期 (format: "12月25日")
  - 剩餘天數 (format: "還有 X 天")
  - 產品縮圖
    ↓
用戶可以:
  • 長按項目 → 刪除
  • 向左滑動 → 快速刪除
  • 點擊項目 → 查看詳情/編輯
```

---

## 🔧 API 集成細節

### Gemini Vision API

**端點:** `/api/gemini` (Next.js API Route)

**請求:**
```json
{
  "image": "base64-encoded-image-string",
  "mimeType": "image/jpeg"
}
```

**回應:**
```json
{
  "product_name": "義美小泡芙",
  "expiry_date": "2025-12-25",
  "confidence": 95,
  "raw_text": "識別的原始文本..."
}
```

**提示詞:**
```
你是一個專業的食品識別助手。
根據上傳的食品包裝照片，提取以下信息：

1. 產品名稱 (例如: "義美小泡芙")
2. 有效期限 (必須是 YYYY-MM-DD 格式，例如 2025-12-25)
   - 如果只看到月份和年份，假設為該月最後一天
   - 如果看到「2025年12月」，轉換為 2025-12-31
3. 信心度 (0-100，表示你對識別結果的確定程度)

回應必須是有效的 JSON 格式：
{
  "product_name": "...",
  "expiry_date": "YYYY-MM-DD",
  "confidence": 95,
  "notes": "任何額外信息或警告"
}

如果無法識別，回應：
{
  "product_name": "",
  "expiry_date": "",
  "confidence": 0,
  "notes": "無法識別圖片中的食品信息"
}
```

---

## 📋 環境變數配置 (.env.local)

```env
# Firebase 配置 (來自 Firebase Console)
NEXT_PUBLIC_FIREBASE_API_KEY=xxx
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=xxx.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=xxx
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=xxx.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=xxx
NEXT_PUBLIC_FIREBASE_APP_ID=xxx

# Gemini API Key (需要從 Google AI Studio 獲取)
GEMINI_API_KEY=xxx

# Vercel 部署標記
NEXT_PUBLIC_VERCEL_ENV=development
```

---

## 🚀 實現階段與里程碑

### 階段 1: 基礎設置 (Day 1)
- [ ] 初始化 Next.js 15 項目
- [ ] 配置 Firebase + Firestore
- [ ] 設置 Tailwind CSS 和全局樣式
- [ ] 建立基本文件結構

### 階段 2: 相機 UI (Day 2)
- [ ] 設計首頁布局 (相機按鈕 + 統計)
- [ ] 實現圖片上傳功能
- [ ] 圖片預覽和壓縮

### 階段 3: Gemini 集成 (Day 3)
- [ ] 設置 API 代理路由
- [ ] 實現 Gemini API 調用
- [ ] 建立確認對話框

### 階段 4: 數據持久化 (Day 4)
- [ ] Firestore 讀寫操作
- [ ] 本地 IndexedDB 緩存
- [ ] 實時同步邏輯

### 階段 5: 庫存儀表板 (Day 5)
- [ ] 構建食品列表組件
- [ ] 實現顏色編碼 (紅/黃/綠)
- [ ] 排序和篩選邏輯

### 階段 6: UI/UX 優化 (Day 6)
- [ ] 優化字體和按鈕尺寸
- [ ] 提升對比度和顏色可訪問性
- [ ] 刪除/編輯交互優化

### 階段 7: PWA + 部署 (Day 7)
- [ ] 配置 PWA manifest
- [ ] Service Worker 設置
- [ ] 部署到 Vercel

---

## ⚠️ 關鍵考慮事項

1. **離線支援:** 使用 IndexedDB 本地緩存，支援飛行模式
2. **圖片存儲:** 先上傳到 Firebase Storage，再存儲 URL 到 Firestore
3. **API 配額:** Gemini API 有免費配額限制，監控使用量
4. **隱私:** 不儲存用戶個人信息 (初期無帳戶)，清晰的資料收集政策
5. **性能:** 圖片壓縮到 < 2MB，延遲加載清單
6. **錯誤處理:** 友善的錯誤提示 (非技術術語)

---

## 📚 資源連接

- [Next.js 文檔](https://nextjs.org/docs)
- [Firebase Console](https://console.firebase.google.com)
- [Google AI Studio (Gemini API)](https://ai.google.dev)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Vercel 部署指南](https://vercel.com/docs)

---

## 🎯 成功標準 (Definition of Done)

✅ 用戶能通過單一按鈕上傳食品照片
✅ AI 自動識別產品名稱和有效期限
✅ 清單按過期日期自動分類 (紅/黃/綠)
✅ UI 簡潔易用 (清晰字體、高對比、易點擊按鈕)
✅ 支援離線使用 (本地數據持久化)
✅ 部署到 Vercel，可通過公開 URL 訪問
✅ 在 iOS 和 Android 上都能正常運行

---

**Last Updated:** 2025-12-15
**Lead Architect:** Claude Code 🤖
